require "test_helper"

class CartControllerTest < ActionDispatch::IntegrationTest
  # test "should update cart" do
  #   patch order_item_url(@order_item), params: { order_item: { order_id: @order_item.order_id, price: @order_item.price, product_id: @order_item.product_id, quantity: @order_item.quantity, subtotal: @order_item.subtotal } }
  #   assert_redirected_to order_item_url(@order_item)
  # end
end
